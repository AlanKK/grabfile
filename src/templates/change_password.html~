{% extends "layout.html" %}
{{ super() }}
{% block content %}
<h2>Change Password for {{ user }}</h2>
  {% if error %}<p class=errormsg><strong>Error:</strong> {{ error }}{% endif %}
  <form action="{{ url_for('save_password', username=user) }}" method=POST>
    <dl>
      <dt>New password:
      <dd><input type=text name=password>
      <dd><input type=submit value=Submit>
    </dl>
  </form>
{% endblock %}

