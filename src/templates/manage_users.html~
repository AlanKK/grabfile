{% extends "layout.html" %}
{{ super() }}
{% extends "admin_header.html" %}
{{ super() }}
{% block content %}
	<h2>Manage Users</h2>
	{% if error %}<p class=errormsg><strong>Error:</strong> {{ error }}{% endif %}
	{% if info  %}<a>{{ info }}</a>{% endif %}
	<ul id="users">
		<form action="{{ url_for('delete_users') }}" method=POST>
		<table>
				<tr>
				<th>Delete&nbsp;&nbsp;&nbsp;</th>
				<th>User&nbsp;&nbsp;&nbsp;</th>
				<th>Type&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;</th>
				<th></th></tr>
			{% for user in users %}
			<tr>
				<td><input type=checkbox name="{{user[0]}}"></td>
				<td>{{ user[0] }}</td>
				<td>
					{% if user[1] %} Admin {% else %} Normal {% endif %}
				</td>
				<td><a href={{url_for('change_password',user=user[0])}}>Change password</td>
			</tr>
			{% endfor %}
		</table>
		<dd><input type=submit value=Delete>
{% endblock %}

